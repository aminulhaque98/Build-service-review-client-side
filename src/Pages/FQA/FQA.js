import React from 'react';
import useTitle from '../../hooks/useTitle';

const FQA = () => {
    useTitle('Fqa');
    return (
        <div>
            <h1>this is fqa page</h1>
        </div>
    );
};

export default FQA;